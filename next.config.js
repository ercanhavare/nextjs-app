/** @type {import('next').NextConfig} */
const nextConfig = {
    // for import external img
    images:{
        domains:[
            "images.pexels.com",
            "example.com",
        ]
    }
}

module.exports = nextConfig
